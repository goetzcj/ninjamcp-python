# NinjaRMM MCP Server Configuration

# Required: NinjaRMM OAuth2 credentials
NINJARMM_CLIENT_ID=your_client_id_here
NINJARMM_CLIENT_SECRET=your_client_secret_here

# Optional: NinjaRMM instance URL (defaults to app.ninjarmm.com)
NINJARMM_BASE_URL=https://app.ninjarmm.com

# Optional: Authentication mode (client, user, hybrid)
# - client: Use client credentials only (machine-to-machine)
# - user: Use user authorization only (requires browser)
# - hybrid: Use client credentials for most operations, user auth for tickets
NINJARMM_AUTH_MODE=hybrid

# Optional: OAuth2 scopes for client credentials flow
NINJARMM_CLIENT_SCOPES=monitoring management control

# Optional: OAuth2 scopes for user authorization flow
NINJARMM_USER_SCOPES=monitoring management control

# Optional: Port for OAuth2 redirect during user authorization
NINJARMM_USER_REDIRECT_PORT=8090

# Optional: Path to store authentication tokens
NINJARMM_TOKEN_STORAGE_PATH=./tokens.json
